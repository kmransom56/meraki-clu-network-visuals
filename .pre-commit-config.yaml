
repos:
  - repo: local
    hooks:
      - id: dependency-drift-check
        name: Dependency Drift Check
        entry: bash -c 'pipreqs . --force --encoding=utf-8 --ignore .venv,scripts,tests && git diff --exit-code requirements.txt'
        language: system
        pass_filenames: false
